
## Status

Not tested yet, project in early development.


## Requirements

- System user `aowis-server` MUST exist
- Port `8120` MUST be available

## Installation

The user `aowis-server` needs to exist on the system:

```
sudo useradd --system --no-create-home --shell /usr/sbin/nologin aowis-server
```

