#!/usr/bin/make -f

%:
	dh $@ --buildsystem=cmake+ninja

override_dh_auto_configure:
	dh_auto_configure --buildsystem=cmake+ninja -- -DAOWIS_SERVER_MAP_BUILD_SERVER=ON -DCMAKE_BUILD_TYPE=Release

override_dh_auto_install:
	:
